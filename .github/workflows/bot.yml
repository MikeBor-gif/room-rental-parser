name: bot

# Обработка сообщений/кнопок бота в режиме long-polling: прогон живёт ~4.7 мин
# и слушает Telegram (getUpdates timeout=25) — ответы приходят через секунды.
# Основной триггер — cron-job.org, дёргающий workflow_dispatch каждые 5 минут:
# новый прогон отменяет предыдущий, слушатель почти всегда жив. schedule — резерв.
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: read

# ВАЖНО: только этот workflow вызывает getUpdates (Telegram допускает один
# активный вызов, параллельный получает 409). cancel-in-progress гарантирует,
# что два прогона не будут опрашивать Telegram одновременно, а идемпотентность
# по last_update_id в БД делает отмену на середине безопасной.
concurrency:
  group: bot
  cancel-in-progress: true

jobs:
  updates:
    runs-on: ubuntu-latest
    # Больше BOT_LOOP_SECONDS (280 с) + подъём машины, иначе прогон умрёт по таймауту.
    timeout-minutes: 7
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-bot.txt

      - name: Install dependencies (light)
        run: pip install -r requirements-bot.txt

      - name: Process updates
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          ADMIN_CHAT_ID: ${{ secrets.ADMIN_CHAT_ID }}
          PAYMENT_DETAILS: ${{ secrets.PAYMENT_DETAILS }}
          LOG_LEVEL: INFO
          # Необязательные настройки тарифов (Settings -> Variables):
          TARIFF_PRICE_BYN: ${{ vars.TARIFF_PRICE_BYN }}
          FREE_BATCH_MINUTES: ${{ vars.FREE_BATCH_MINUTES }}
          PREMIUM_MAX_FILTERS: ${{ vars.PREMIUM_MAX_FILTERS }}
          BOT_LOOP_SECONDS: ${{ vars.BOT_LOOP_SECONDS }}
        run: python -m src.jobs.updates
